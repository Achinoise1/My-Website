# 安装与部署

## 安装

安装 next.js 可能会有版本要求，因为是在 Windows 下，不能使用 `n` 进行管理。这里直接换装高版本 node。

## 运行

注意，运行使用的是 `npm run dev`，而不是 `npm run start`，`npm run start` 跑出来的是 `npm run build` 的内容。另，如果使用的代码编辑器是 VSCode，创建 Next.js 项目后，左侧会出现一个名为 `NPM SCRIPTS` 的内容。编写代码后可以直接找到 dev，点击即可进行编译，会比在终端运行的速度快。

## 